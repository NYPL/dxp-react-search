services:
  node:
    image: tugboatqa/httpd
    commands:
      build:
        - apt-get update
        - apt-get install nodejs
        - apt-get install npm
        - npm install
        - npm run build
        - npm start &
        - ln -snf "${TUGBOAT_ROOT}" "${DOCROOT}"
