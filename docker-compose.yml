version: '3.7'

services:
  finder:
    container_name: finder
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3333:3000
    environment:
      - CHOKIDAR_USEPOLLING=true