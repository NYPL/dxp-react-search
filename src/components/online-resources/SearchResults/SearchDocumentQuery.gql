query SearchDocumentQuery(
  $q: String,
  $tid: String,
  $alpha: String,
  $limit: Int,
  $pageNumber: Int,
  $offset: Int
) {
  allSearchDocuments(
    limit: $limit,
    pageNumber: $pageNumber,
    offset: $offset,
    filter: {
      q: $q,
      tid: $tid,
      alpha: $alpha
    }
  ) {
    items {
      ... on OnlineResourceDocument {
        id
        slug
        name
        description
        accessibilityLink
        termsConditionsLink
        privacyPolicyLink
        notes
        updateFrequency
        printEquivalent
        format
        language
        outputType
      }
    }
    pageInfo {
      totalItems,
      limit,
      pageNumber,
      pageCount,
      timestamp
    }
  }
}